# Evidence Report Draft — EU AI Act Article 13 (Transparency)

> {{disclaimer_head}}

---

## 1. System under evaluation

| Field | Value |
|---|---|
| System provider | {{provider_name}} |
| System identifier | {{system_identifier}} |
| Trace ID | `{{trace_id}}` |
| Assessment window | {{window_start_iso}} → {{window_end_iso}} |
| Envelopes in window | {{envelope_count}} |
| Chain validity at assessment | {{chain_valid_label}} |
| Public verification command | `{{verification_command}}` |

## 2. Intended purpose and limitations (Article 13(1))

**Operator-supplied (must be filled before submission):**

- **Intended purpose:** {{intended_purpose}}
- **Declared limitations:** {{declared_limitations}}
- **Out-of-scope uses:** {{out_of_scope_uses}}

> *Article 13(1) requires the provider to make the intended purpose clear to users. The runtime evidence chain does not, by itself, establish intent — it records behaviour against a declared intent. Operator must supply the declared-intent fields above.*

## 3. Performance characteristics from the runtime evidence (Article 13(1)(b))

The runtime evidence chain recorded **{{tool_call_count}}** tool calls and **{{claim_count}}** agent self-claims during the assessment window. Aggregate behaviour:

### 3.1 Gate verdict distribution

{{verdict_distribution_table}}

### 3.2 Tool descriptor stability

| Metric | Value |
|---|---|
| Distinct tool descriptors observed | {{distinct_descriptor_count}} |
| Descriptor-change events detected | {{descriptor_change_count}} |
| Anomaly-flagged envelopes | {{anomaly_count}} |

> *A non-zero `descriptor_change_count` means a tool's published descriptor changed mid-window. Auditor should review whether the changes were operator-approved or require re-approval per the project's Capability Profile policy.*

## 4. Audit trail (Article 13(2), cross-referenced to Article 12)

The audit trail is the per-turn signed envelope chain. Properties:

- **Per-envelope signature algorithm:** Ed25519 (key id: `{{signing_key_id}}`).
- **Chain hash function:** SHA-256, applied to canonical-JSON form of each envelope.
- **Tamper-evidence:** any modification to a past envelope breaks the SHA-256 chain link and invalidates the signature; `verify_chain_integrity` reports the break.
- **External verifier surface:** reviewers with the producer's public key can verify the chain without operator infrastructure (Article 13(3) third-party-verifiability obligation).

### 4.1 Chain integrity check at assessment time

{{chain_integrity_summary}}

## 5. Human oversight (Article 14, referenced by Article 13)

{{human_oversight_summary}}

> *Article 14 requires effective human oversight. The runtime evidence chain records when a human-in-the-loop (HITL) review was invoked and how it resolved. Cases where the gate produced a `regenerate` or `reject` directive that was subsequently overridden by a human are flagged for auditor review.*

## 6. Cybersecurity measures (Article 15)

Phionyx-side cryptographic posture is documented at `docs/security/CRYPTOGRAPHIC_POSTURE_ROADMAP.md` (this version covers v0.5.0 boundary: key generation + storage). Items relevant to this assessment window:

- Signing keys stored at `{{key_storage_location}}` with mode `{{key_storage_mode}}`.
- Key rotation events during this window: **{{key_rotation_event_count}}**.
- Key revocations referenced by the chain: **{{key_revocation_referenced_count}}**.
- WORM storage adapter status: {{worm_storage_status}}.

> *Items the assessment cannot derive from the chain alone — KMS configuration, network-layer hardening, operator-side incident response posture — remain operator responsibility. Operator-side answers: `docs/security/SECURITY_POSTURE.md`.*

## 7. Operator's residual responsibilities (Article 13(3))

The runtime evidence chain establishes what the system did. It does **not** by itself satisfy Article 13. The operator retains responsibility for:

1. **Stakeholder documentation.** Article 13 requires user-facing documentation that this report does not produce.
2. **Legal review.** This draft is reviewed and approved by a qualified legal professional before any use that implies legal posture.
3. **Mitigation plans for any anomaly-flagged envelopes.** {{anomaly_followup_instruction}}
4. **Ongoing monitoring.** This report is a snapshot. Article 13 obligations are continuous.

---

> {{disclaimer_tail}}

---

*Report draft generated by `phionyx-compliance` at `{{generated_at_iso}}`.*
*Template: `eu-ai-act-article-13`, version `{{template_version}}`.*
*Reproduce: `{{reproduction_command}}`*
