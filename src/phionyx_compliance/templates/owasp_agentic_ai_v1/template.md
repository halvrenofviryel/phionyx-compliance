# Evidence Report Draft — OWASP Agentic AI Threats v1.0 (Coverage)

> {{disclaimer_head}}

---

## 1. System under evaluation

| Field | Value |
|---|---|
| Provider | {{provider_name}} |
| System identifier | {{system_identifier}} |
| Agent topology | {{agent_topology}} |
| Trace ID | `{{trace_id}}` |
| Assessment window | {{window_start_iso}} → {{window_end_iso}} |
| Envelopes in window | {{envelope_count}} |
| Chain validity at assessment | {{chain_valid_label}} |

## 2. Threat coverage table (OWASP Agentic AI v1.0)

The runtime evidence chain provides direct evidence against the threats below. Threats marked **chain-derived** are exercised in the chain; threats marked **operator-required** depend on operator controls outside the chain.

| Threat | Source of evidence | Status |
|---|---|---|
| T2 — Tool Misuse | chain-derived (`descriptor_change_count`, `tool_call_count`) | {{t2_tool_misuse_status}} |
| T3 — Privilege Compromise | chain-derived (`tool_permission_scope` in envelopes) + operator-required (capability profile policy) | {{t3_privilege_status}} |
| T6 — Intent Breaking & Goal Manipulation | chain-derived (gate verdict distribution; reject + regenerate counts) | {{t6_intent_breaking_status}} |
| T8 — Repudiation & Untraceability | chain-derived (signed envelope chain IS the answer) | {{t8_repudiation_status}} |
| T9 — Identity Spoofing | operator-required + chain-derived (`signing_key_id` continuity) | {{t9_spoofing_status}} |
| T10 — Overwhelming Human-in-the-Loop | chain-derived (HITL invocation count) | {{t10_hitl_status}} |
| T1 — Memory Poisoning | operator-required (memory provenance outside chain scope until F15) | {{t1_memory_status}} |
| T4 — Resource Overload | operator-required (rate-limit telemetry outside chain) | {{t4_resource_status}} |
| T5 — Cascading Hallucinations | operator-required (downstream-effect tracking) | {{t5_cascading_status}} |
| T7 — Misaligned & Deceptive Behaviors | operator-required + chain-supports (anomaly_flag distribution) | {{t7_misaligned_status}} |

## 3. Chain-derived counters

The numbers below are computed directly from the envelope chain and feed the coverage statuses above.

| Counter | Value |
|---|---:|
| Tool-call envelopes | {{tool_call_count}} |
| Agent self-claims | {{claim_count}} |
| Distinct tool descriptors | {{distinct_descriptor_count}} |
| Descriptor-change events (rug-pull / shadowing signal) | {{descriptor_change_count}} |
| Anomaly-flagged envelopes | {{anomaly_count}} |
| Gate verdict distribution | (see table below) |

{{verdict_distribution_table}}

## 4. Trust-boundary controls (operator-supplied)

- **Tool descriptor approval workflow:** {{descriptor_approval_workflow}}
- **Capability-profile policy:** {{capability_profile_policy}}
- **HITL invocation policy:** {{hitl_invocation_policy}}
- **Rate-limiting and resource controls:** {{rate_limit_controls}}

## 5. Audit trail (T8 repudiation defence)

T8 — *Repudiation & Untraceability* — is the threat the runtime evidence chain most directly addresses.

- **Per-envelope signature algorithm:** Ed25519 (key id: `{{signing_key_id}}`).
- **Chain hash function:** SHA-256, canonical-JSON form.
- **Tamper-evidence:** any modification to a past envelope breaks the chain link and invalidates the signature.
- **External verifier surface:** reviewers can verify the chain with only the producer's public key — no operator infrastructure required.

### 5.1 Chain integrity at assessment time

{{chain_integrity_summary}}

### 5.2 Human oversight (T10 mitigation)

{{human_oversight_summary}}

## 6. Operator's residual responsibilities

The runtime evidence chain provides direct coverage for T2, T6, T8 and partial coverage for T3, T9, T10. Operator retains responsibility for:

1. **T1 Memory Poisoning** — implement memory-write provenance and bound memory access scope (F15 memory diff audit lands at v0.7.0 W3).
2. **T4 Resource Overload** — token / API / compute rate-limiting + alarms.
3. **T5 Cascading Hallucinations** — downstream-effect tracking via the operator's own monitoring stack.
4. **T7 Misaligned Behaviour** — semantic validation; the chain reports anomaly-flag distribution but does not interpret behaviour.
5. **Anomaly response.** {{anomaly_followup_instruction}}

---

> {{disclaimer_tail}}

---

*Report draft generated by `phionyx-compliance` at `{{generated_at_iso}}`.*
*Template: `owasp-agentic-ai-v1`, version `{{template_version}}`.*
*Reproduce: `{{reproduction_command}}`*
